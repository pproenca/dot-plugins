schema_version: "1.1"
name: forge-resumes-mid-workflow

context:
  # Staged into the case's isolated working directory before the run.
  # Requires --scaffold; the runner will not execute it otherwise.
  scaffold_script: scaffold.sh
