---
type: file_exists
path: ontology/contracts/inbound/*.odcs.yaml
---

The contract must exist as a file, in the inbound directory, with the .odcs.yaml extension the
plugin's own validator globs for. A contract that only appears in the response is not written.
