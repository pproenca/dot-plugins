---
type: file_exists
path: ontology/AUDIT.md
---

The audit writes its ranked report to a file and summarizes only the critical findings in the
response. A report existing solely in the response is the failure the file-output rule exists to
prevent, and it is checkable without judgement.
