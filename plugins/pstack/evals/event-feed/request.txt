Our event feed sometimes returns short or empty pages even when more matching events exist. It also loses events that share a timestamp when users move to the next page. Fix page() in events.py so tenant and status filters, both sort directions, and cursor pagination follow the README contract. Keep the public API, use only the standard library, and add focused regression tests for the behavior you fix.
